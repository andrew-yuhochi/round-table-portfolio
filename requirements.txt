# round-table-portfolio — pinned dependencies
# SQLite is stdlib (no pip install needed).
# Data-source libraries will be added as TASK-M1-003 is implemented.

# Testing
coverage==7.14.1
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pygments==2.20.0
pytest==9.0.3
pytest-cov==7.1.0
